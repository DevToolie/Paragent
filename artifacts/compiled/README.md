# artifacts/compiled/

Compiled trajectory bundles produced by B3 (`npm run compile`).

Each `*.bundle.json` is a `compiled_trajectory` package: one
`contracts/cache-row.schema.json` row per step. See `docs/gate/compiler.md`.
